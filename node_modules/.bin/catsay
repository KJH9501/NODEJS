#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  "$basedir/coffee"  "$basedir/../catsay/bin/catsay" "$@"
  ret=$?
else 
  coffee  "$basedir/../catsay/bin/catsay" "$@"
  ret=$?
fi
exit $ret
